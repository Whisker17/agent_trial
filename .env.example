### Mantle AaaS Gateway Environment Variables ###

# ===========================================
# PROXY BYPASS (required on macOS with system proxy)
# ===========================================
NO_PROXY=*

# ===========================================
# PLATFORM (Required)
# ===========================================
# Encryption key for agent wallet private keys (AES-256-GCM)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PLATFORM_ENCRYPTION_KEY=

# Server port (default: 3000)
# PORT=3000

# Database path (default: data/gateway.db)
# DATABASE_PATH=data/gateway.db

# ===========================================
# LLM PROVIDER - OpenRouter (Required for agents)
# ===========================================
# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key-here
OPENROUTER_MODEL=arcee-ai/trinity-large-preview:free
OPENROUTER_SMALL_MODEL=arcee-ai/trinity-large-preview:free
OPENROUTER_LARGE_MODEL=arcee-ai/trinity-large-preview:free
OPENROUTER_EMBEDDING_MODEL=openai/text-embedding-3-small
OPENROUTER_EMBEDDING_DIMENSIONS=1536

# ===========================================
# MANTLE RPC (Optional overrides)
# ===========================================
# Mantle Mainnet RPC (Chain ID: 5000)
ETHEREUM_PROVIDER_MANTLE=https://rpc.mantle.xyz
# Mantle Sepolia Testnet RPC (Chain ID: 5003)
ETHEREUM_PROVIDER_MANTLE_SEPOLIA_TESTNET=https://rpc.sepolia.mantle.xyz

# ===========================================
# PRIVY (Required for authentication)
# ===========================================
# Get your App ID and App Secret from https://dashboard.privy.io/
# Frontend (exposed to browser via Vite)
VITE_PRIVY_APP_ID=your-privy-app-id
# Backend (server-side JWT verification)
PRIVY_APP_ID=your-privy-app-id
PRIVY_APP_SECRET=your-privy-app-secret
# Development fallback: if Privy verification fails, allow decoded JWT payload.
# Set to false in production-like testing to enforce strict verification.
PRIVY_ALLOW_UNVERIFIED_TOKENS=true

# ===========================================
# LOGGING
# ===========================================
LOG_LEVEL=info
